// Shared by WeekView, DayView, and TimeGridEventBlock so the
// pixel math for drag/resize stays in sync with what's rendered.
export const HOUR_ROW_HEIGHT = 48; // px per hour row
export const MIN_BLOCK_HEIGHT = HOUR_ROW_HEIGHT / 4; // 15-minute floor
